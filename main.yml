- name: "Configuring machine"
  hosts: localhost
  roles:
    - role: homebrew
      tags: homebrew
  tasks:
    - name: Displaying message that finished
      ansible.builtin.debug:
       msg: "Done!"
